// ternary operator 
// ternary operator to check the eligibility to vote
let n=parseInt(prompt(`enter your age`));

let votingAge=(n>=18 ? `you are elligible` : `you are not eligible to vote`);
 console.log(votingAge)